Отладка с помощью gdb: Если вы используете GCC, выполните:

bash
gcc -g -o a.out your_program.c
gdb ./a.out

В gdb используйте:
run -yoilne "Игнорирует" -e "dcs" 1.txt

Когда произойдёт сбой, выполните команду:
backtrace

Она покажет, где в коде произошла ошибка
